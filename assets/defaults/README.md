# Default Images

This folder contains the default images that are displayed in the app before users upload their own images.

## Usage
- Place image files (JPG, PNG, etc.) in this folder
- These images will be used as placeholders in the application
- Images should be optimized for web use